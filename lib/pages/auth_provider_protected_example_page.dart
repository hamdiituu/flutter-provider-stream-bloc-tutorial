import 'package:flutter/material.dart';

class AuthProviderProtectedExamplePage extends StatelessWidget {
  const AuthProviderProtectedExamplePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Protected Page'),
      ),
    );
  }
}
